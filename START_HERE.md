# START HERE —— UR10 CB3 单关节微动预实验与正式实验一体化工具

**版本 1.0.3**（2026-09-24，实验室候选版）。相对 1.0.2 的六处改动见 README 的
"版本说明"；现场最需要先知道的两条是：**分组处理并删除 RAW 现在默认开启**，
以及**连设备后会先做一次 5 s 全屏采集检查，这一步机械臂不动**。

一个窗口、三个主按钮：**连设备并到位 → 采静态噪声 + 三档预实验 → 离线分析**，
然后在界面上确认六个正式步长，跑组 A（单向阶梯）和组 B（换向）。

---

## 这是什么，不是什么

**是**：一个把"已经跑通过的相机采集 / RTDE 通信 / 离线角点处理"串起来，
按需求三/五的时间顺序**编排**动作、采集、记账、分析的工具。

**不是**：
* 不是碰撞检查工具。**没有碰撞模型**，`collision_status` 永远是 `unknown`。
* 不是"一键全自动"。到位过程**每一步都要人工确认**，中止后**不会自动回位**。
* 不替你做科学判断。推荐步长只是"满足判据的最小幅度"，判据和阈值都能在界面上改。

## 一分钟上手

| 顺序 | 双击 | 会发生什么 |
|---|---|---|
| 1 | `install_deps.bat` | 建 `.venv`、装依赖、自检版本（不连任何硬件） |
| 2 | `run_dry_run_example.bat` | 用**合成数据**把整套流程跑一遍，约两分钟，结果落在 `outputs\` |
| 3 | `run_ui.bat` | 打开界面。默认干运行；要真机得自己切模式并明确确认 |

`run_ui.bat` 会优先加载 `configs\local_site.json`（如果存在），否则用
`configs\experiment_default.json`。现场参数改完请"另存为" `configs\local_site.json`
（这个文件名已在 `.gitignore` 里，不会跟着工具流转）。

## 三个按钮

**① 连接设备并到达实验姿态**
检查依赖 / 磁盘 / 输出目录 → 连接机器人（CB3）与相机 → 预热 →
**先做 5 s 全屏采集检查（此时机械臂一动不动）**，量实际分辨率、实际帧率、
缺帧比例、实际 RAW 字节数、实际写盘速度，不通过就**直接停在这里**并说清原因 →
读当前状态 → 按当前实际姿态生成 **6 个关节空间中间点** →
**每个点都等你确认后才低速走一步**，并打印"这一步相对**当前**姿态要动多少" →
到位后给一张棋盘格预览图、检查 11×8 内角点是否全部检出、余量是否够。

**② 采静态基线 + 三档预实验**
先录 10～20 s 静止（默认 15 s，可改）算静态噪声与漂移；再做**逐关节**的快速几何确认
（用 0.05° 正负各试一次：棋盘格完整、画面明显动了、方向大致符合理论、主要是面内运动、
没有严重掉帧），不过关就停下来问你。然后三档幅度（默认 0.01 / 0.05 / 0.2°）
× 正负两个方向 × 每方向 2 次 = 每关节 12 次、六个关节共 72 次，
每次动作都从名义位姿**独立出发**（不累计），每一次都有独立事件编号；
回程数据照样保存，但不计入统计。**关节之间停下来等你确认，关节内部自动**；
默认开启删除 RAW 时，**每个关节采完就停住、处理、校验、删 RAW，再走下一个关节**
（六轴合一的"快速几何检查"是唯一例外，它整段算一组）。

**③ 离线分析 → 确认步长 → 跑正式实验**
按关节 / 幅度 / 方向给出：指令变化量、RTDE 实际变化量、棋盘格视觉位移、重复性、
正负差异、死区与量化、超调与振荡、RTDE 稳定后画面是否还在动，
以及**推荐步长和理由**。推荐值会填进界面上那六个输入框，**可以人工改**。
六档幅度都不够时，只会说"需要人工填写更大步长"——**本工具不会自动外扩到 1° 或 5°**。

确认步长后跑组 A（阶梯 + 原路下来，去程/回程分别分析）和组 B（换向实验）。
跑之前会按名义运动学做一次范围检查；**理论检查不等于碰撞安全**，界面会明说
collision 仍是 unknown。

## 现场必须填/核对的参数

参数面板里每一项都能改，改完记得"另存"。**上真机前至少核对这几项**：

| 参数 | 默认值 | 说明 |
|---|---|---|
| `robot.ip` | `192.168.125.12` | 取自被复用的旧预实验代码（那套在真机上跑通过）。不是这个就改 |
| `camera.serial` | 空 | 海康相机序列号 |
| `camera.mvs_import_path` | 空 | MVS SDK 的 Python 示例目录（不填则用不到真相机） |
| `camera.analysis_roi` | 空 | 离线分析窗口（**只影响去哪里找角点，不影响 RAW 尺寸**）。空 = 整幅分析 |
| `camera.exposure_us` / `camera.gain` | 空 | 留空表示用相机当前值 |
| `paths.delete_raw_after_process` | **true** | v1.0.3 起默认开启：每组处理并校验通过后删掉该组 RAW |
| `pretest.amplitudes_deg` | `[0.01, 0.05, 0.2]` | 三档幅度 |
| `formal.step_deg` | `{}` | 六个正式步长，按钮三会填推荐值，可人工改 |
| `robot.trial_speed_deg_s` | 0.5 | 必须 ≤ `robot.max_speed_deg_s`（5.0），否则界面拒绝保存 |
| `robot.trial_accel_deg_s2` | 1.0 | 必须 ≤ `robot.max_accel_deg_s2`（10.0） |
| `paths.output_root` | `outputs` | 输出根目录；每次运行在下面建时间戳子目录，永不覆盖 |

## 时间与磁盘（诚实数字）

界面一打开就会把这场实验的规模算出来打印在日志里。默认配置（整幅 1936×1096
@ 132.23 fps ≈ 280 MB/s、六个关节、组A+组B 都跑、正式步长 0.2°）大约是：

* **整场 RAW 约 953 GB**（分组流水线下**盘上峰值**只有最大那一组的 **38.49 GB**）
* 正式步长 0.05° 时整场约 530 GB，最坏一组 19.56 GB

按阶段看，每一组的实测尺寸是（RAW / 峰值，峰值已含处理期临时余量）：

| 组 | RAW | 峰值 |
|---|---|---|
| 静态基线（15 s） | 3.92 GB | 4.73 GB |
| 快速几何检查（六轴合一组） | 14.70 GB | 17.75 GB |
| 三档预实验（每关节一组） | 12.32 GB | 14.87 GB |
| 正式组A（每遍 repeat 一组） | 15.94 GB | 19.25 GB |
| 正式组B（每遍 repeat 一组） | 31.88 GB | 38.49 GB |

**RAW 一律整幅，ROI 不参与磁盘估算**：`camera.analysis_roi` 只决定离线去哪里
找角点，改它不会让 RAW 变小。上面这些数就是按整幅算的。

### 连上设备之后的 5 s 全屏采集检查

连上机器人和相机之后、**任何运动命令之前**，先采 5 s 整幅 RAW
（`camera.connection_check_s`），量五件事：实际原始分辨率、实际帧率、
帧号是否连续（缺帧数与比例）、实际 RAW 字节数、实际每秒写盘速度。
通过就删掉这次测的 RAW，只留 `connection_check/` 下的汇总 JSON/TXT 和少量样本图；
**不通过就禁止任何运动**，并中文说清是**帧率不足**、**掉帧**还是**磁盘写入不足**。
门槛都能改：`camera.min_fps_ratio`（0.95 × 期望帧率）、`camera.min_write_headroom`
（1.2 × 相机满速字节率）、`camera.max_dropped_ratio`。换完镜头或 ROI 之后，
界面上的"重做 5 s 全屏采集检查（不动机器人）"按钮可以在不重开会话的情况下重测。

### 分组处理并删除 RAW（v1.0.3 起**默认开启**）

`paths.delete_raw_after_process` 默认 `true`，界面顶上一行会写着
`分组处理并删除RAW：已开启`。流程按**组**走：

```
采集一组动作的 RAW                    ← 一组 = 静态基线 / 快速几何检查（六轴合一）/
→ 机械臂停止在名义姿态                    预实验的一个关节 / 正式组A或组B 的一个关节的**一遍 repeat**
→ 刷新并暂停 RTDE 采样（处理期一行不写）
→ 完整处理这一组（逐帧角点、质心、二维转角、尺度、时间戳、RTDE、本段分析）
→ 按 analysis_roi 离线识别角点（导出坐标已加回 ROI 偏移，是原图坐标）
→ 回读校验（六条）
→ 校验通过才删掉这一组的 RAW，JSON 里 raw_available 由 true 改成 false
→ 恢复 RTDE 采样，相机先丢 resume_drain_frames 帧并重建帧号基线
→ 才进入下一遍 / 下一组运动
```

所以盘上任何时刻只有**当前这一组**的 RAW 加派生数据。正式组A/组B 都是
**每一遍 repeat 单独一组**，不会把"5 循环 × 2 方向 × 3 遍"塞成一组。
处理期间**不会发出任何运动命令**；校验不过就保留 RAW、`raw_available` 仍是 `true`、
说明文字里不会出现"RAW已删除"，会话停在那里等人。

**两条线比的是"同时驻留峰值"**（RAW + 派生 + 处理期临时余量）：
`paths.max_peak_disk_gb`（默认 **50 GB**）是硬上限，任何一组的估算峰值超过它
就**不允许开始这一组**，一条运动命令都不会发出去（清完盘还能接着用这个会话）；
`paths.disk_warn_gb`（默认 **40 GB**）是软件预警线。单遍仍超 50 GB 时只在
"**已经回到名义姿态**"的边界上再拆一级；阶梯中途拆不动就**事先拒绝并说明原因**，
绝不为了过闸门少走几步、少重复几遍或降帧率。
`paths.min_free_disk_gb`（默认 2 GB）只是绝对下限，真正把关的是按组估算。
每一组开始前，界面都会打印：本组动作数量、预计录制秒数、预计帧数、预计 RAW 大小、
预计峰值、是否低于 40/50 GB、本组结束后是否自动删 RAW。

★ **处理步长**：`paths.process_stride` 默认 **1**（逐帧，132 Hz）。
删除 RAW 之前**必须**已经生成逐帧的角点和视觉结果，所以正式保留数据的路径
固定用 1；`4` 只留给现场快速预览（界面上的"复算"可以传 `--stride`），
**不能**用它做删除前的最终提取。

★ 不许关掉 `camera.save_raw`：**本工具不提供这个选项**，走的是"必须留证据"的
路线。盘实在不够时把输出根目录换到更大的盘（`paths.output_root`）；
改 `camera.analysis_roi` **没有用**——RAW 永远是整幅，磁盘估算也按整幅算。
真要压小，只能靠上面那套"在名义姿态处再拆一级"的分组规则，
或者关闭 `paths.delete_raw_after_process` 之外……没有别的口子。

★ 边采边清是有代价的，必须知道：RAW 一旦删掉，**只能**用当时存下的角点与
逐帧几何复算，**不能换检测参数重新识别像素**（比如事后想调阈值、换角点检测器）。
所以删之前要**六条**全过才动手：

1. 有效帧比例 ≥ `thresholds.min_valid_frame_ratio`；
2. `capture_metadata.json` 里的相位表在，且运动前 / 运动 / 保持 / 回程 / 运动后
   每个阶段都有足够的有效帧（只认出"中间那些帧"不算）；
3. `segment_vision.json` 和角点 CSV 能回读，且回读值与刚算完的一致；
4. 相机时间戳 CSV 在；
5. 对应的 RTDE 行在；
6. 本段的基本分析能正常跑完。

**任一条不过就保留 RAW、写 `raw_kept`、暂停等你处理**，绝不"少删一个文件了事"。
段失败、中途停止、停稳超时同样一律保留 RAW。想做参数扫描的研究，就把它关掉。
删除前后 JSON 里的 `raw_available` 怎么变（先写 `true` → 校验 → 删 → 确认盘上没了
→ 原子改写 `false`）写在 README 的"删 RAW 的顺序本身就是数据安全"一节里。
删除之后，同一个会话里紧接着的快速几何检查和 J6 角度**不会**再回头读
`frames.raw`——它们只复用已经算好并存下的逐帧结果。

以上都是**估算**：真机上控制器怎么规划、现场要不要重来，都会让它变长。

## 上真机之前

1. 装真机通信库：`.venv\Scripts\python.exe -m pip install ur_rtde`
   （干运行/回放一次都不会导入它，自测里有一条专门断言这件事）。
2. 装海康 MVS SDK，并把它的 Python 示例目录填进 `camera.mvs_import_path`。
3. 按需求二的摆放提示摆相机：**水平放置、约 675 mm、方位角约 33.3°、
   棋盘格与相机正视方向偏差约 14.75°**。这些**只是摆放提示，不是碰撞验证**。
4. 界面切到 `hardware` 必须有人明确点头；默认配置永远是 `dry_run`。
5. 每一步运动之前，界面都会把目标关节角打出来；到位过程逐步确认，急停随手可及。

## 干运行的三个已知局限

干运行（合成世界）能验证流程、判据、落盘和界面，但有三件事它做不到：

1. 机器人一开始就在名义位姿上，所以**到位过程的 6 步是零位移**——真机上才会真的走。
   因此干运行里的 **J6 快速确认带有随机性**：0.2° 在合成世界的成像尺度下接近可分辨
   极限，同样的配置有时判"以面内为主"、有时判"看不出来"。真机上棋盘格成像大得多，
   这个抖动不会出现；但**不要**拿干运行里 J6 的那一次结论当准。
2. 合成棋盘格只有 16 px/格（192×144 的板），所以 **J6 的 0.05° 快速确认会说"看不出来"**。
   真机上棋盘格成像大得多。这不是缺陷，是合成世界的尺度。
3. 合成数据 **不能**用来推断任何真实结论——报告里凡是合成数据都会被标 `synthetic`。
   5 s 全屏采集检查在干运行里也是合成帧，所以它量到的"实际帧率/写盘速度"不是真机数字，
   只用来验证这条链路本身跑得通。

## 出事了怎么办

* 任何时候按**中止**：正在跑的这一段会停下来，**不会自动回位**，机器人停在哪就是哪。
* 棋盘格丢失或余量不足会自动暂停并问人，不会自己重试。
* 掉帧、断连、放弃的段都会记进 `events.jsonl` / `missing_frames.csv`。
* **分析失败不影响原始数据**：RAW、时间戳、元数据都已在盘上，
  可以用 `reanalyze`（界面上的"复算"）重算，复算结果写新目录，**绝不覆盖**原结果。

## 目录

```
install_deps.bat / run_ui.bat / run_dry_run_example.bat   三个入口
configs/experiment_default.json                           默认配置
docs/dry_run_example/                                     一次真实干运行的快照（文本为主）
docs/reused_modules.md                                    复用了哪些原始模块
docs/messages/                                            BAT 里的中文提示（BAT 本身必须是纯 ASCII）
src/sj_pretest/                                           本工具的代码
src/sj_pretest/vendor/                                    一行未改的被复用代码
tests/                                                    自测（不连任何硬件）
outputs/                                                  每次运行的时间戳目录（不进版本库）
```
